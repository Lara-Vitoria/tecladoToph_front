import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: { 
        flex: 0.1,
        height: "100%"
      },
      bottom: {
        position: 'relative',
        bottom: -30,
        right: 20
      },
});

export default styles